<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        .note {
        position: absolute;
        top: 50%;
        animation: move 6s linear infinite;
        font-size: 2rem;
        }

        /* Different speeds for variety */
        .note:nth-child(2) { animation-duration: 8s; top: 40%; }
        .note:nth-child(3) { animation-duration: 10s; top: 60%; }

        @keyframes move {
        from { left: 100%; transform: rotate(0deg); }
        to { left: -10%; transform: rotate(360deg); }
        }
    </style>
</head>s
<body>
    {{!-- add images, start detecting button, musical notes span --}}
    <img src="img/instrument_img.jpeg" alt="instrument image"> 
    <div>
        <button>Start Detecting</button>
    </div>
    <div>
        <div class="note">🎵</div>
        <div class="note">🎶</div>
        <div class="note">🎼</div>
    </div>
</body>
</html>